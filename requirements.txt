# 로컬 실행용 (Colab은 대부분 기본 제공)
# 검증 기준 버전 — 상위 호환 허용(하한 표기)
# 검증 기준 버전: torch 2.4.1 / transformers 4.44.2 / sentence-transformers 3.0.1 / numpy 1.26.4
#   — 결정론적 출력이 필요한 12-3만 노트북 내 인라인 핀(transformers==4.44.2) 사용
#   — 12-3 생성 모델: Qwen/Qwen2.5-3B-Instruct (12-2는 1.5B)
numpy>=1.26  # 검증 기준 1.26.4 (상위 호환 허용)
matplotlib>=3.9
scikit-learn>=1.5
pandas>=2.2
# 9장 이후 (딥러닝 실습)
tensorflow>=2.17
# 11·12장 (트랜스포머·LLM 실습)
transformers>=4.44
torch>=2.4
sentence-transformers>=3.0  # 12-3 RAG 미니 (임베딩)
# 13장 (강화학습)
gymnasium>=1.0
